const requestAuthUrl = `https://api.login.yahoo.com/oauth2/request_auth`;
const accessUrl = `https://api.login.yahoo.com/oauth2/get_token`;
const refreshUrl = `https://api.login.yahoo.com/oauth2/request_auth`;

export async function requestAuthToken() {
	// const res = await fetch(requestUrl);
	console.log("request yahoo auth token");
}

export async function requestAccessToken() {
	// const res = await fetch(requestUrl);
}

export async function refreshAccessToken() {
	// const res = await fetch(requestUrl);
}
